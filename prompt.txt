Always carry this prompt in your context, and read it explicitly every time.
Examine this entire diff: `git --no-pager diff d5d8cef4d3834cc8999eb9344066b0960b033f2d`. The changes to the logic in Binding.zig was done to improve the keybind resolution on Linux, and it worked perfectly with GDK's nice native ability to track when a modifier is consumed to type a character. Config.zig explains the goal of the changes very well. A rogue AI agent has tried to improve the macOS side but failed, changing so many things very invasively.
The changes don't work on macOS as well as on Linux. The goal is to make bindings intuitive and consistent as described in the Config.zig document (accurate right now to Linux) and work across Greek, French Canadian, Devanagari, German, US, and more.
The Swift/macOS side is a mess. Caps Lock and the Ctrl key, both of which can modify characters in some layouts in unique ways, are being incorporated into the code in a cursory way that doesn't respect the uniqueness of macOS keyboard layouts and does not cover nearly as many edge cases as the concise logic that processes data from GDK flawlessly.
Furthermore, on macOS, the Terminal Inspector isn't even displaying that bindings are being ran, even when they execute fine.
It's also concerning how many comments and how much infrastructure in the macOS side, which was done by AI, has been removed, and not surgically.
This project uses Nix managed by direnv, so always run build commands from the root directory and use direnv exec . <command> if tool paths are missing, and xcode 26.5 should be working fine.
Carefully analyze **every single clause** in this prompt. Carefully analyze every file mentioned in these search results, and research beyond it to uncover hidden infrastructure your surgical macOS changes will make. Take a great deal of time explaining how the codebase before the changes since d5d8cef4d3834cc8999eb9344066b0960b033f2d has worked to understand Ghostty input at a deep level, keeping track of things in a .md file in the root of the git dir (make that file untracked).
The old macOS logic also meant that Ctrl Shift = would trigger the increase font size by 1, when it keybinds should not behave that way, so Ctrl + should be added to the macOS default keybinds list, wherever that is, to maintain continuity for defaults.
